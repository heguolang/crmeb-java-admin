# 预发布 / 灰度环境（可提交）
# 使用：npm run build:stage（与现有脚本一致，mode=staging）

NODE_ENV=production
VUE_APP_ENV=staging

VUE_APP_BASE_API=https://staging-api.example.com
VUE_APP_WS_URL=wss://staging-api.example.com
